/*
$(document).ready(function(){
    var nav = $('.menubox');
    var headoffset = $('.menubox').offset();
    $(window).scroll(function () {
        if ($(this).scrollTop() >= headoffset.top) {
            nav.css('top',0).addClass('position','fixed');
        }else {
            nav.css('position','absolute');
            nav.css('top',0).css('position','absolute');
        }
    });
});
*/